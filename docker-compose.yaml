services:

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"


  zoo-app:
    build: .
    container_name: zoo-app

    ports:
      - "8090:8090"
    depends_on:
      - redis
    volumes:
      - ./uploads:/app/uploads
